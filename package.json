{
  "name": "bs-jsonwebtoken",
  "version": "0.0.1",
  "description": "Bucklescript bindings for jsonwebtoken",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && bsb -make-world",
    "watch": "npm run clean && bsb -make-world -w",
    "clean": "rm -rf lib && bsb -clean-world"
  },
  "keywords": [
    "bucklescript",
    "reason",
    "reasonml",
    "jwt"
  ],
  "license": "MIT",
  "author": "Ryan Biwer",
  "_": [],
  "repository": {
    "type": "git",
    "url": "git@github.com:ryb73/bs-jsonwebtoken.git"
  },
  "peerDependencies": {
    "bs-platform": "^4.0.0"
  },
  "devDependencies": {
    "jsonwebtoken": "^8.3.0"
  }
}
